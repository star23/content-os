---
type: profile
scope: longterm
note: This file is the hard constraint layer. The agent reads it but must never write to it.
---

# <Your name>'s long-term positioning

> Fill this in yourself. The agent treats everything here as a **hard constraint** —
> it overrides anything inferred from recent performance.

## One-line identity

<e.g. "An investor-operator writing at the intersection of AI, crypto and public markets.">

## Reader profile

- Who they are:
- What they come to you for:
- What they will not tolerate from you:

## Your cross-domain edge (the differentiator)

Describe the 2–3 domains you can credibly bridge. This is what the agent looks for
when it flags "cross-domain opportunities" — the highest-priority signal class.

- Domain A × Domain B —
- Domain B × Domain C —

## Core tracks

List 4–6 tracks, ordered by how well they perform for you. These ids must match the
`tracks[].id` values in `topic_library.json`.

| id | name | why you own it |
|---|---|---|
| `track-one` | | |
| `track-two` | | |

## Thinking anchors

People and frameworks you routinely reason from. The agent uses these to keep drafts
sounding like you rather than like generic commentary.

- **<Person>** — <the idea you actually use>
- **<Framework>** — <...>

## Writing style guide

### Voice
- <e.g. "Confident, not grandiose — few exclamation marks, more statements and questions.">
- <e.g. "Analytical on the surface, opinionated underneath.">

### Opening hooks (durable meta-patterns)
1. <pattern>
2. <pattern>

### Sentence and rhythm rules
- <e.g. "Short paragraphs. No more than one idea per line.">

## Red lines (hard prohibitions)

**The agent must never violate these, regardless of what recent performance suggests.**

- Never <e.g. "give direct buy/sell advice">
- Never <e.g. "post about a position without disclosing you hold it">
- Never <e.g. "use engagement-bait formats: threads that withhold the point">
